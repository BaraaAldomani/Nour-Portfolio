<?php

declare(strict_types=1);

// Global helpers are defined here and registered through composer's `autoload.files`.
// Implementations land in the infrastructure phase; see App\Services\SiteContent.
